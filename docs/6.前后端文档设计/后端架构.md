# 后端架构

## 技术选型

- Node.js + TypeScript
- NestJS 框架（开启 CORS）
- PostgreSQL + Drizzle ORM
- 阿里云 OSS（图片存储）
- AI 多模态：阿里百炼 / 火山大模型 / MiniMax 2.7（三方可切换）
- yt-dlp + ffmpeg（视频处理）
- cheerio（淘宝页面解析）

## 目录结构

```
backend/src/
├── app.module.ts              # 根模块
├── main.ts                    # NestJS 入口
├── wardrobe/                  # 衣橱模块
│   ├── wardrobe.controller.ts
│   ├── wardrobe.service.ts
│   └── dto/
├── outfit/                    # 搭配模块
│   ├── outfit.controller.ts
│   ├── outfit.service.ts
│   └── dto/
├── recognize/                 # 图片识别模块
│   ├── recognize.controller.ts
│   └── recognize.service.ts
├── video/                     # 抖音视频模块
│   ├── video.controller.ts
│   └── video.service.ts
├── taobao/                    # 淘宝解析模块
│   ├── taobao.controller.ts
│   └── taobao.service.ts
├── common/                    # 公共
│   ├── services/
│   │   ├── ai/                # AI 多模型抽象层
│   │   │   ├── ai.interface.ts            # 模型统一接口
│   │   │   ├── alibaba-bailian.provider.ts  # 阿里百炼实现
│   │   │   ├── volcano.provider.ts        # 火山大模型实现
│   │   │   ├── minimax.provider.ts        # MiniMax 2.7 实现
│   │   │   └── ai.module.ts               # NestJS 动态模块，根据配置切换
│   │   ├── oss.service.ts     # OSS 上传
│   └── db/
│       ├── schema.ts          # Drizzle 表结构
│       └── database.module.ts # PG 连接模块
└── scenes/                    # 场景模块
    ├── scenes.controller.ts
    └── scenes.service.ts
```

## API 清单

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/recognize | 图片识别衣物 |
| GET | /api/wardrobe | 获取衣橱列表 |
| POST | /api/wardrobe | 添加衣物（图片存 OSS） |
| PUT | /api/wardrobe/:id | 编辑衣物元信息 |
| GET | /api/wardrobe/:id | 单品详情 |
| POST | /api/video/parse | 抖音视频解析 |
| POST | /api/video/save | 保存视频提取的衣物 |
| POST | /api/taobao/parse | 淘宝链接解析 |
| GET | /api/scenes | 场景列表 |
| POST | /api/outfit/recommend | 搭配推荐 |
| POST | /api/outfit/evaluate | 自选搭配 AI 评价 |

> 注意：无 DELETE 接口，防止恶意清空共享数据。

## CORS 跨域配置

前端 (Vite dev server `localhost:5173`) 和后端 (NestJS `localhost:3000`) 端口不同，需要开启 CORS。

```typescript
// main.ts
const app = await NestFactory.create(AppModule);
app.enableCors({
  origin: ['http://localhost:5173'],  // 开发环境前端地址
  methods: ['GET', 'POST', 'PUT'],
  credentials: false,                  // 无用户体系，不需要 cookie
});
```

生产环境部署时将 `origin` 改为实际域名。

## 数据库表

| 表 | 说明 |
|----|------|
| clothes | 衣物（品类、颜色、风格、oss_url、来源） |
| outfits | 搭配方案（JSONB items、评分、理由） |
| scenes | 预置场景（通勤/约会/运动） |

## AI 多模型切换设计

通过 `.env` 环境变量控制使用哪个模型，业务代码不感知具体实现。

```
AI_PROVIDER=alibaba_bailian    # alibaba_bailian | volcano | minimax

# 阿里百炼
ALIBABA_BAILIAN_API_KEY=xxx

# 火山大模型（字节跳动）
VOLCANO_API_KEY=xxx
VOLCANO_ENDPOINT=https://ark.cn-beijing.volces.com/api/v3

# MiniMax 2.7
MINIMAX_API_KEY=xxx
MINIMAX_GROUP_ID=xxx
```

**统一接口：**

```typescript
// ai.interface.ts
export interface IAiProvider {
  // 图片识别：传入图片 URL，返回结构化衣物信息
  recognizeClothing(imageUrl: string): Promise<ClothingInfo>;
  // 搭配推荐：传入衣物列表 + 场景，返回搭配方案
  recommendOutfit(clothes: Clothing[], scene: string): Promise<OutfitPlan[]>;
  // 审美评价：传入选中的衣物，返回评分和建议
  evaluateOutfit(clothes: Clothing[]): Promise<Evaluation>;
}
```

**切换方式：** `ai.module.ts` 根据 `AI_PROVIDER` 环境变量，动态注入对应的 provider。

```typescript
// ai.module.ts
@Module({
  providers: [
    {
      provide: IAiProvider,
      useFactory: (config: ConfigService) => {
        const provider = config.get('AI_PROVIDER');
        if (provider === 'doubao') return new DoubaoProvider(config);
        return new DashScopeProvider(config);  // 默认
      },
      inject: [ConfigService],
    },
  ],
  exports: [IAiProvider],
})
export class AiModule {}
```

各业务模块（recognize、outfit）只注入 `IAiProvider`，不关心底层用的是哪个模型。换模型 = 改 `.env` 一行配置。
